#开发环境配置
server:
  port: 1001

#eureka配置
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false #是否将自己注册到eureka server
    fetch-registry: false #是否从eureka server获取注册的服务信息
